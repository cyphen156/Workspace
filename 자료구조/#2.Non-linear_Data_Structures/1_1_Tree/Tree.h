#pragma once

//#include "#2.Non-linear_Data_Structures/0_Node/Node.h"

namespace NonLinearDataStructures
{
	// Tree class definition
	template <typename T>
	class Tree
	{
	public:
		//// Default constructor
		//Tree() : root(nullptr) {}

		//// Constructor with initial value
		//Tree(const T& value) : root(new Node<T>(value)) {}
		//
		//// DeepCopy constructor
		//Tree(const Tree& other) : root(other.root ? new Node<T>(*other.root) : nullptr) {}
		//~Tree() { delete root; }
		//
		//Tree& operator=(const Tree& other)
		//{
		//	if (this != &other)
		//	{
		//		delete root;
		//		root = other.root ? new Node<T>(*other.root) : nullptr;
		//	}
		//	return *this;
		//}

	private:
		//Node<T>* root; // Root node of the tree
	};
}